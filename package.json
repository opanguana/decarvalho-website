{
  "name": "decarvalho-website",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "serve . --listen 5173",
    "preview": "serve . --listen 4173",
    "lint": "node tools/check-project.mjs --lint --format",
    "format:check": "node tools/check-project.mjs --format",
    "test": "node tools/check-project.mjs --smoke && npm run responsive:check",
    "responsive:check": "node tools/check-responsive.mjs",
    "build": "node tools/build-static.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "serve": "^14.2.5"
  }
}
