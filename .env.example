# No environment variables are required for the current static site.
# Copy this file to .env only when future integrations need local-only values.
# Never commit real secrets.

# Future contact form endpoint, if a backend or form provider is added.
CONTACT_FORM_ENDPOINT=

# Future analytics identifier, if privacy-approved analytics are added.
ANALYTICS_ID=
